#ifndef HTABLE_H_
#define HTABLE_H_
/**
 * Two very slow insertion and merge sorts.
 * The combination, however, is bareable
**/

extern void insertion_sort(int *a, int n);
extern void merge_sort(int *a, int *w, int n);

#endif
